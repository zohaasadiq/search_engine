const crypto = require("crypto"); const payload = require("./test_event.json"); const webhookSecret = "whsec_xMUDO69Ei6W0Wdlvlh8u36RkVnjcjbVK"; const timestamp = Math.floor(Date.now() / 1000); const signedPayload = `${timestamp}.${JSON.stringify(payload)}`; const signature = crypto.createHmac("sha256", webhookSecret).update(signedPayload).digest("hex"); console.log(`t=${timestamp},v1=${signature}`);
